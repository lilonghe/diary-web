:global {
    .button{
        margin-top: 10px;
        background-color: #108ee9;
        line-height: 28px;
        height: 28px;
        padding: 0 15px;
        border: 0;
        outline: none;
        color: #FFF;
        border-radius: 4px;
        cursor: pointer;
    }
}